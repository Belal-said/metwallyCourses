Credentials
      1- Username
      2- Password
      3- Token
            3.1- Encrypted json object or Random text
            3.2- It expires, so it must be expire every amount of time or with an action

Authorization: Basic <user&password base64 string>